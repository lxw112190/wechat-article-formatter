# Changelog

## 未发布

## v0.5.0 — 2026-08-20

- 增加真正的 Word `.docx` 导出，支持标题、正文、强调、代码、列表、任务列表、表格、图片和超链接，并根据当前主题生成配色、字体与标题装饰。
- 优化 Word 定稿版式，增加适合阅读和打印的页边距、段落行距、引用卡片、代码块、表格内边距、图片说明及标题页脚页码。
- Word 导出增加可保存的设置面板与三套快速模板，支持 A4/A5、页边距、字体字号、行距、标题、目录、页脚页码和图片宽度配置。
- 增加 Word 兼容 HTML 导出，复用当前主题与页面设置，自动内嵌可读取图片；可用 Word/WPS 打开后另存为 `.docx`，用于追求更接近网页/PDF 的外观。
- Word 导出增加重复 H1 去重、H1～H3 可点击目录、中文分页保护、长链接换行、合并单元格、自动列宽，以及 WebP/SVG 图片兼容转换。
- 完整 ZIP 备份升级为 V5，备份和恢复 Word 导出设置，并继续兼容 V2～V4 备份。
- Word 导出使用纯前端 Office Open XML；本地或远程图片读取失败时自动降级为图片说明，不阻断整篇文档生成。
- 微信公众号文章链接保持标准 `<a>`，不伪造微信私有属性，由微信自行识别和规范化。
- 普通外链、HTTP 链接和 `mailto` 链接在公众号复制时降级为可见文字与 URL，避免微信过滤链接后地址丢失。
- GFM 任务列表在公众号复制时转换为 `☑ / ☐`，多级嵌套列表转换为稳定的扁平块，避免内容顺序被重排。
- 升级正文链接发布前检查，区分公众号文章链接、普通外链、HTTP 链接和邮箱链接。

## v0.4.0 — 2026-08-12

- 文章与历史本地存储升级为带版本的数据结构，继续兼容旧数组格式；遇到损坏或更高版本数据时保留原始副本并暂停覆盖。
- 草稿库增加标题/正文搜索、更新时间与标题排序、文章置顶和复制。
- 删除文章改为移入回收站，支持恢复、彻底删除和清空；彻底删除后才清理对应历史与无用图片。
- 增加存储空间中心，展示浏览器配额、文章、历史、图片和未使用图片占用，并支持申请持久化存储。
- 完整 ZIP 备份升级为 V4，包含回收站文章及 Markdown，并继续兼容 V2/V3 备份。
- 完整 ZIP 恢复增加文章、回收站、历史、主题、设置和图片的回滚处理。
- 增加 Playwright Chromium 端到端测试与 CI 步骤，覆盖自动保存、回收站、草稿管理、主题持久化、主题文件、完整备份和图片 ID 复制。

## v0.3.1 — 2026-08-11

- 修正应用、安装包和文档中的版本号。
- 补充 v0.3.0 的正式发布记录。

## v0.3.0 — 2026-08-11

- 增加完整的自定义主题编辑器，支持配色、字体、正文密度、标题、引用、代码块、表格、图片、分隔线和圆角配置，并提供实时预览。
- 支持独立主题文件导入与导出，主题文件经过格式版本和字段范围校验，可安全分享给其他用户。
- 手机预览、公众号正文复制、HTML 导出和打印 PDF 统一使用完整主题样式。
- 完整 ZIP 备份与恢复现在包含浏览器中的全部自定义主题。
- 主题编辑器增加基础/高级模式、可折叠分组、微软雅黑等 10 类字体、H1～H6 独立设置、正文、列表、强调、链接和图片说明配置。
- 增加手机与桌面预览切换，以及撤销、重做和恢复功能。
- 主题文件格式升级为 V2，并兼容导入已有 V1 主题文件。

## v0.2.0 — 2026-07-22

- 将原 1499 行的 `App.tsx` 拆分为独立 UI 组件和业务 Hooks，`App.tsx` 仅保留应用编排。
- 增加 GitHub Actions 持续集成与标签发布工作流。
- 完整 ZIP 备份升级为 V3：逐文件 SHA-256 校验，并限制压缩包容量、解压总容量、单文件容量和条目数量；保留 V2 备份只读兼容。
- 增加 ZIP 内容篡改与容量限制测试。
